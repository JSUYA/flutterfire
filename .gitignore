# Miscellaneous
*.log
*.pyc
.DS_Store
.svn/
.idea/
.vscode/

# Flutter/Dart/Pub related
# Libraries should not include pubspec.lock, per https://dart.dev/guides/libraries/private-files#pubspeclock.
/pubspec.lock
**/doc/api/
.dart_tool/
.packages
build/

# Tools
tools/**/*.jar

# pubspec_overrides.yaml is per-developer local dev tooling
# (path overrides for sibling packages). Keep it out of publish/commit.
pubspec_overrides.yaml
pubspec.lock
