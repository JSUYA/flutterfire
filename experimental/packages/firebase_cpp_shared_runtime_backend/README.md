# firebase_cpp_shared_runtime_backend

Experimental backend that keeps a shared native C++ runtime process alive and forwards Firebase requests through it.

Implemented scope:

- initialize
- email/password sign-in
- callable Functions requests
