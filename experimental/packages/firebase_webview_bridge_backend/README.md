# firebase_webview_bridge_backend

Experimental backend that uses a WebView-compatible JavaScript bridge and the Firebase JavaScript SDK.

Implemented scope:

- initialize
- email/password sign-in
- callable Functions requests
