name: firebase_webview_bridge_backend
description: Experimental WebView and JavaScript bridge backend for Firebase on Tizen.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  firebase_backend_contract:
    path: ../firebase_backend_contract

dev_dependencies:
  lints: ^5.1.1
  test: ^1.26.3
